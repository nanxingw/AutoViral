# PRD-0008 issue 切片 · 折叠镜表（Shot Sheet）

Tracker 状态：docs-only（不建 GitHub issue）。标签语义见 docs/agents/triage-labels.md。
全部切片 `ready-for-agent`。依赖链：T1 →（T2 ↔ T3 同文件串行）→ T4 → T5。

## T1 · sceneEdit.ts 增删 wrapper ✅ scope
**文件**：`web/src/features/studio/panels/AssetSidebar/sceneEdit.ts` + 其测试。
**做**：补 `addSceneRemote(workId, {title})`（POST `/api/bridge/v1/scene`，沿用现有 bridgeFetch/错误处理模式）与 `removeSceneRemote(workId, sceneId)`（DELETE `/api/bridge/v1/scene/:id`）。server 路由已 wired，纯客户端 wrapper（各 ~8 行）。
**验收**：单测覆盖 happy/HTTP 失败；与现有 patchScene 错误协议一致。

## T2 · 折叠镜头条 + 手风琴展开 Inspector（核心两态）
**文件**：`ScriptTab.tsx`、`web/src/i18n/messages.ts`、`ScriptTab` 测试。
**做**：SceneCard 拆两态——折叠态一行只读摘要（镜号/状态点+「需重生」文字徽章/标题/时长/景别/意图，已生成附 28px 缩略图）；点击行展开为卡内 Inspector（现有全部字段控件 + ↑↓ + stale 警示 + 生成 CTA + ✓已存微反馈），手风琴 state（同时仅 1 个 expandedSceneId）。保留：inline-commit、focus 防顶掉、null-clear、生成防重入、draggable 整卡拖拽。
**验收**：折叠态无表单控件；展开互斥；三态徽章可断言（文字非颜色）；既有行为测试不回归。

## T3 · ⋯ 菜单 + 新建/删除
**文件**：同 T2（同文件，与 T2 串行实现）。
**做**：每行 hover/focus 显 ⋯ 菜单（上移/下移/删除两步确认——参照 #66 模式，不抢全局 Delete 键）；列表底部「＋ 新建分镜」+ 空状态主按钮 → T1 的 addSceneRemote（title 用本地化占位文案，新卡自动展开聚焦标题）；删除 → removeSceneRemote。
**验收**：新建后卡片出现且自动展开；删除需两步、第二步前可取消；空状态有可点按钮（替换纯文案）。

## T4 · 剧本折叠条
**文件**：同 T2。
**做**：ScriptEditor 外包一层 ▾ 折叠开关（默认展开，记 localStorage），收起态显「▸ 剧本」一行；展开态为现有编辑器原样（edit/preview/错误行全保留）。
**验收**：折叠状态切换 + 刷新记忆；收起时分镜列表上移。

## T5 · 绿门 + 多纬度 E2E + docs
**做**：`npm run test:web` 全绿 + web typecheck 0；`build:frontend` + 重启 daemon 后跑轻量 3 纬度浏览器 E2E Workflow（人 UI 全流程 / agent CLI `scene add/remove` 同步一致 / 边界：空状态·stale 徽章·删除取消），截图 + DOM 二确；CHANGELOG `[0.1.7]` 段 + PRD 翻 Shipped。
