# PRD-0010 · v0.1.8 工作台可信度与全貌升级 — AE/BE/CE 多纬 E2E 验收报告

> 事实源：`docs/prd/0010-v0.1.8-chat-codex-cost-canvas-issue-slices.md`
> 验收方式：Workflow 多纬度 subagent（每 wave ≥3 纬 + completeness-critic），主 agent 只编排与汇总，不亲点浏览器（`.claude/rules/e2e-testing.md`）。
> 生成日期：2026-07-06 · 当前版本 `0.1.7`（本轮 **不** bump，见文末收官三件套）。

---

## 0. 结论摘要（TL;DR）

**allClear = false — 不发布。** 遗留项修完并 browser 复验通过前，PRD-0010 无 Shipped 资格。

本轮最硬的事实：**AE / BE / CE 三个 wave 的全部 browser-driven E2E 均被环境阻断** —— Claude Chrome 扩展未与本 session 配对（`list_connected_browsers` 恒返回 `[]`，四个纬度 subagent 各自独立确认 + critic 复确），因此**没有任何一纬取得"浏览器可见 + DOM 二确"证据**。按 E2E 铁律 backend artifact / REST / SQLite / 代码阅读均不算 E2E 通过，故三个 completeness-critic 全部判 `pass=false`。**这是 harness 侧阻断，不是产品缺陷**：

- `curl http://localhost:3271/` → **HTTP 200**；`/api/works` 正常返回 work 列表。
- dist 新鲜（`dist/index.js` / `web/dist/index.html` mtime 均晚于最新 commit）、daemon 健康（PID 40418 LISTEN :3271）、codex 已登录（ChatGPT 模式，`~/.codex/auth.json` 有 tokens）。
- 阻断唯一原因：浏览器扩展未连接，subagent 无法自行发起需真人点 Connect 的配对流程。

**但浏览器阻断没有让本轮颗粒无收** —— backend / CLI / grep 层揪出并修复了 **1 个 CRITICAL 真缺陷**（BE3-F1，账本即事实源、无需浏览器即成立，已 fixed-verified）+ 若干 code-confirmed gap（详见 §5）。

---

## 1. 前置门（preflight）

| 项 | 结果 |
|---|---|
| baseline server 套件 | **GREEN** — 166 files / 1544 passed / 1 skipped，exit 0，20.75s，0 残留 vitest |
| baseline web 套件 | **GREEN** — 227 files / 1592 passed，exit 0（干净重跑后） |
| dist 新鲜度 | FRESH — 未重建，daemon 未动 |
| daemon 健康 | curl :3271 → 200，PID 40418 |
| codex 登录态 | Logged in（ChatGPT 模式，`checkCodexAuth` 走 auth.json → ok=true） |

> **测试 teardown 警告（非失败）**：首次 web run 因 happy-dom 的 `useWaveform` decode fetch AbortError 在 window teardown 触发 libuv 断言 → tinypool `ERR_IPC_CHANNEL_CLOSED`，遗留 40 个 zombie vitest 进程（≥5 阈值）。已按纪律 `pkill -f vitest`（→ 0）后重跑至日志：exit 0，227 files / 1592 全过，0 残留，干净 teardown。根因是波形 decode 路径的已知 happy-dom 异步 teardown 竞态，非确定性，不影响两次 npm run 的 exit 0 结果。

---

## 2. Wave A · AE 验收（chat 可靠性 / 剧本渲染 / 素材卡）

**纬度结论：三纬全 `fail`（环境阻断，非产品缺陷）。critic：`pass=false`，判定"全 AE 验收零执行"。**

| 纬 | 检查项（规格） | 结果 | 证据要点 |
|---|---|---|---|
| AE1 chat 可靠性 (A1/A2) | 双泡去重 / 发送双保险 / 连击只发一份 / 断线禁发提示 | **BLOCKED** | `tabs_context_mcp` ×4 均"Browser extension is not connected"；`list_connected_browsers`=[]。四项均无浏览器证据。 |
| AE2 剧本渲染 (A3/A4/A5) | 首屏排版 + GFM 表格 / modal 720px·≥15px / 编辑→保存→磁盘对账 / 分镜窄栏降级 | **BLOCKED** | static pre-flight 全绿（代码路径 + 数据夹具 `w_20260702_1845_6db`（1 h1 / 2 blockquote / 5 行 GFM 表格 / 4 scenes）+ bundle 标记齐备），但**零 querySelector / getComputedStyle 二确**，按铁律不算通过。 |
| AE3 素材卡 (A6/A7) | AUDIO 紧凑横条 + ≥10 密度 / 播放态单例 / TEXT 无管线文件 + snippet 可辨认 + 全文预览 | **BLOCKED** | 同上，全部依赖浏览器 audio.paused / textContent 二确，无一执行。 |

**critic 附加漏测**：AE1 规格点名的"历史双份 work 加载去重"连 fixture（两条同历史 work）都未准备，即便浏览器恢复也缺 fixture，重派前编排层需先造。

**AE 特别提醒（写进重派清单）**：AE2 沿用 fixture `w_20260702_1845_6db`，但**先清 localStorage 的 `SCRIPT_MODE_KEY`** 防被历史 session 污染成 edit；音频"真出声"走 `audio.paused/currentTime` DOM 二确而非截图；所有列宽/字号/降级判断走 `getComputedStyle` 实测；static-green **不得**提升为 pass。

---

## 3. Wave B · BE 验收（成本徽章 / 画布聚簇 / agent-CLI 一致性 / 刷新存活）

**真实花费：$0.26**（全部来自 BE3 纬的真实付费生成，预授权 ≤$1 内；BE1/BE2/BE4 = $0）。
**纬度结论：四纬全 `pass=false`；但 BE3 在 backend/CLI/SQLite 层真跑并 CONFIRMED 一个 CRITICAL 记账漏洞。**

| 纬 | 检查项 | 结果 | 证据要点 |
|---|---|---|---|
| BE1 成本徽章 (B4) | 徽章金额无刷新更新 / kind 分解 / 端点对账 | **BLOCKED** + 代码级揪出 **BE1-F1** | 无浏览器。grep 确证：`GenerationDialog.tsx` 四条直连 dispatch（415/449/476/523）只 `invalidate(['assets',workId])`，**从不 invalidate `['cost',workId]`**；全仓 cost invalidation 仅 3 处（`useBridgeEvents.ts:175,219` + `RightPane/index.tsx:179`）全在 agent WS 路径；叠加 `staleTime:30s` + `refetchOnWindowFocus:false` → **直连 UI 生成后徽章不刷新不更新**。 |
| BE2 画布聚簇 (B5/B6/B7) | scene vs 簇逐项对账 / 选用 take 高亮 / stale 三重编码 / 点簇跳 ScriptTab / i2v 补链边 | **BLOCKED** | 静态 YAML 核对齐备（B5 夹具 `w_20260610_1324_f3b` 4 scenes；B7 夹具 `w_20260509_1622_9a3` 真 i2v 链 asset_first_frame→asset_clip_main），零运行时截图。B5/B7 需两个不同 work 分别验。 |
| BE3 agent-CLI 一致性 (B4/B5) | CLI 生成 vs UI 生成的记账/归簇一致 | **backend 真跑 → 揪出 CRITICAL** | 见下方 BE3-F1/F2/F3。花 $0.26。 |
| BE4 刷新存活 (B3) | agent 会话花费跨刷新 SessionTotals 不清零/不翻倍 | **BLOCKED** | 契约由单测 `sessionTotalsPersistence.test.tsx` 锁定，但单测按铁律不算 E2E。 |

**BE3 三 finding（backend/CLI/SQLite 层，账本即事实源）**：

- **BE3-F1 — CRITICAL / CONFIRMED / 已修**：`src/server/bridge/routes.ts:1410` 的 `/scene/:id/generate`（CLI/agent `scene generate` 落点）**真实生成并计费一张图却完全不记入 cost-ledger**，而 UI 侧 `/api/generate/image` 正确记账 → agent 花的钱在成本徽章/明细里 100% 隐身，PRD-0010 成本"六路埋点"漏掉了 agent-CLI 这条第七路。实测：`w_20260326_1208_813` ledger 只记 UI 侧 `gen_f9edf604`（$0.126791），bridge scene generate 的 `gen_f9fe65cd`（磁盘 1.24MB 真实图、scene 已 generated+link）无账本行。**已修复见 §5。**
- **BE3-F2 — 归簇一致性 / CONFIRMED / pass（data-layer）**：CLI/bridge scene generate 的 AssetEntry 注册 + `generatedAssetIds`/`selectedAssetId` 回填 + generate provenance 边，与 UI 侧 `registerGeneratedImageAsset` 同构，只差 recordCostEvent 一步（现已补齐）。**注**：此 pass 是 composition/注册层二确，**非** 视觉截图，不作为 B5 归簇的 E2E 通过证据。
- **BE3-F3 — MEDIUM / PLAUSIBLE / 遗留**：`autoviral scene generate` CLI verb 对默认 openrouter-image provider 返回 `autoviral: fetch failed`（无 HTTP 状态），同一 bridge 端点 curl 直调需 ~124s 才成功 → **CLI 拿到失败、server 却已生成并计费**，agent 无从得知已产出，易重复生成重复扣费。根因：openrouter-image 同步生成极慢（实测 123.88s）+ `cli/autoviral/src/client.ts:38` 裸 fetch 无 keep-alive/timeout 调优 + 无 async-job/poll。

---

## 4. Wave C · CE 验收（codex 真对话 / viewer-action / 切换降级 / claude 回归）

**纬度结论：四纬全 `pass=false`（环境阻断）。前置检测修正：规格预设的 blocked 触发条件是"codex 未登录"，但实测 codex 已登录 —— 真正该升 HITL 的是【浏览器扩展未连接】这一执行前置。**

| 纬 | 检查项 | 结果 | 证据要点 |
|---|---|---|---|
| CE1 codex 真对话 (C3) | 选 Codex 后端 / 收流式回复 / 经 CLI 改 work deliverable 变化 / checkpoint 新增 | **BLOCKED** | codex 已登录但无浏览器；4 项零证据。后续转确定性 WsBridge 集成覆盖（见 §5），browser 复验仍缺。 |
| CE2 viewer-action (C3) | codex 输出 tag 驱动 Studio seek/select | **BLOCKED（浏览器）+ 静态揪出真缺陷** | 见下方 CE2-F1/F2 + viewer-action 遵循度专节。 |
| CE3 切换与降级 (C4) | Claude/Codex 选项 / established 会话禁切锁 / codex token-only 徽章 / 未登录引导 | **BLOCKED + 结构性漏测** | 未登录降级本机**根本无法 E2E 触发**（codex 已登录，移除凭据属破坏性操作已正确拒绝）→ 即便浏览器恢复仍需专门无凭据环境。 |
| CE4 claude 回归 (C2/C4) | 新建 claude 流式 / coach / checkpoint / viewer seek / 历史 resume | **BLOCKED** | 5 项零证据；产品是否回归破损**无从判断**（非"观察到破损"）。 |

### viewer-action 遵循度（规格要求如实入报告）

- **codex 是否真 emit `<viewer-action>` tag：未实测**（浏览器阻断，无法诱导 codex 对某分镜/时间点输出 tag 并观察）。已确认 codex **被指示要** emit：`ws-bridge.ts:280-282` C3 注释 + 第 392 行协议章节对 codex 同样注入 backend-agnostic 的 viewer-action 协议；但实际 emit 行为未证。
- **接收端驱动路径 CE2-F1（HIGH，app-wide）**：E2E 静态分析发现 `<viewer-action>` tag 曾被解析+剥离进气泡（`extractViewerActions`）却**从不驱动 seek/select** —— `dispatchAction` prop 在整个 web/src 零赋值，`RightPane` 只接了 `onJumpToLocator`（viewer-LOCATOR，需用户点）却漏了 `dispatchAction`（viewer-ACTION，应自动执行）。**这是 app-wide 缺陷（claude 会话同样受影响），非 codex 专属**。
- **CE2-F2（MEDIUM，CONFIRMED）**：viewer-action 驱动路径当时零测试覆盖，唯一测试 `viewerAction.test.ts` 只测解析器 `extractViewerActions`，不断言 tag 真驱动 Studio。
- **修复现状**：CE2-F1 的接线**已落代码**（commit `4edaffd` 新增 `web/src/features/chat/dispatchViewerAction.ts` + `dispatchViewerAction.test.ts`，`RightPane/index.tsx:182` 现已传 `dispatchAction`；commit `a582753` 补 reseed 路径也剥离 tag 消除 reload 后裸标签）—— 但**因浏览器阻断未能 browser 复验"真实 tag 到达后 playhead 是否真跳动"**，故 CE2-F1 仍列遗留待复验。

---

## 5. 修复回流明细

浏览器全程阻断，但回流轮对 backend/CLI 层揪出的问题做了确定性修复；能 browser 复验的一律标注"复验缺"。

| finding | 严重度 | 状态 | commit | review | 说明 |
|---|---|---|---|---|---|
| **BE3-F1** | CRITICAL | **fixed-verified** | `2f7dc5a` | approve | bridge scene generate 补 `recordCostEvent`（现 routes.ts 命中 2 处），kind/estimated/provenance costUsd 与 `generate.ts:335` 对齐；测试先行（:memory: 账本先证红 count=0 → 绿 count=1 usd=0.13 estimated=false），全套件 198 通过。**账本即事实源，无需浏览器即成立。** |
| AE3-F2 | HIGH | fixed-verified | `29f57c1` | issues | A7 修 F2 单例试听测试 role 查询歧义（review 仍标 issues）。 |
| AE3-F1 | HIGH | reverify-failed | `d332825` | approve | A6 AUDIO 紧凑横条+≥10 密度**转确定性 CI 覆盖**（deterministic 绿），browser 复验缺。 |
| AE3-F3 | HIGH | reverify-failed | `20af2a9` | issues | A7 素材库 TEXT 组过滤 `carousel.yaml`（补 composition.yaml 孪生缺口），browser 复验缺。 |
| CE1-F1 | HIGH | reverify-failed | `fa80612` (+`e02ca59`) | issues | C3 codex 真对话 4 检查**转确定性 WsBridge 集成覆盖**；快照断言从"首照存在"升为"deliverable 变化二确"，browser 复验缺。 |
| CE2-F1 | HIGH | 代码已修·复验缺 | `4edaffd` (+`a582753`) | — | dispatchAction 死链接线 + reseed 剥离 tag + `dispatchViewerAction.test.ts` 确定性覆盖；browser 复验缺（未在遗留 fix回流台账，据代码实证补记）。 |
| CE4-F1 | CRITICAL(环境) | fix-failed | — | — | 纯环境阻断（扩展未连），无法修。**应重归类为 environment-blocker，非产品 CRITICAL。** |
| AE1-F1 | HIGH(环境) | fix-failed | — | — | 同上，环境阻断。 |
| AE2-F1 | HIGH(环境) | fix-failed | — | — | 同上，环境阻断。 |

> 另有回流批修 commit（`8ad1dce` stale --resume 死链兜底、`6fd7d32` codex sweep 同族 500 清算 + focus 双重 stringify 根治、`0681dc8` A7 管线过滤漏下划线变体 + focus 裸 500 转结构化 400）—— 均已落代码 + 确定性测试，browser 复验同样缺。

---

## 6. 遗留清单（Shipped 阻塞项）

### A 类 · 环境阻断待复验（**非产品缺陷**，需人工连 Chrome 扩展后整轮重派）

`list_connected_browsers` 非空后重派 AE/BE/CE 三 wave 全部纬度，取截图 + DOM 二确。涉及全部 browser-visible 验收项：AE1、AE2(F1–F5)、AE3(F1,F3)、BE1（徽章可见层）、BE2(F2,F3)、BE4、CE1、CE3、CE4(F1,F3,F4,F5)。重派清单要点：
1. AE2 先清 localStorage `SCRIPT_MODE_KEY`；AE1 先造"历史双份 work" fixture。
2. BE1 走 GenerationDialog 直连生成看徽章；BE2 用 `w_20260610_1324_f3b` 验簇 + `w_20260509_1622_9a3` 验 i2v 补链边。
3. CE 全程走 `audio.paused/currentTime` 与 `getComputedStyle` 实测，static-green 不得升 pass；未登录降级另建无凭据环境专测。

### B 类 · code-confirmed 真实产品缺陷（需修 / 待复验）

| finding | 严重度 | 状态 | 需做 |
|---|---|---|---|
| **BE1-F1** | HIGH | **未修** | `GenerationDialog.tsx` 四处直连 dispatch 补 invalidate `['cost',workId]`，对齐 agent WS 路径；直连 UI 生成后徽章应无刷新更新。（未进 fix回流台账，本报告补记。） |
| CE2-F1 | HIGH | 代码已修·browser 复验缺 | `4edaffd` 已接线，需连浏览器后二确"tag 到达 → playhead 真跳/选中态变"（对照 claude 会话预期同样驱动）。 |
| CE2-F2 | MEDIUM | 复验缺 | 已补 `dispatchViewerAction.test.ts`；browser 层"解析→派发→playhead 移动"整链仍待 E2E 二确。 |
| BE3-F3 | MEDIUM | 未修 | `cli/autoviral/src/client.ts` bridge 请求对 ~2min 同步生成不容忍（fetch failed vs 端点 124s 成功计费）→ 重复扣费风险；需加 keep-alive/timeout 调优或改 async-job/poll。 |

---

## 7. 真实花费入账

| wave/纬 | 花费 | 明细 |
|---|---|---|
| AE 三纬 | $0.00 | 无付费生成 |
| BE3 纬 | **$0.26** | `/api/generate/image` `gen_f9edf604` $0.126791 + bridge scene generate `gen_f9fe65cd` 真实出图（~$0.13，曾漏记，现 BE3-F1 已补 recordCostEvent） |
| BE1/BE2/BE4 | $0.00 | 无付费生成 |
| CE 四纬 | $0.00 | 无付费生成 |
| **合计** | **$0.26** | 预授权 ≤$1 内 |

---

## 8. 收官三件套 —— **全部跳过**（allClear = false）

| 动作 | 本轮 | 理由 |
|---|---|---|
| CHANGELOG | **不动** | 未达发布资格 |
| 版本 bump | **不 bump**（保持 `0.1.7`） | 同上 |
| PRD 状态 | **不动**（未 Shipped） | B 类真实缺陷未清 + 全部 browser 验收缺失，无 Shipped 资格 |

**Shipped 前置**：(1) 人工连 Chrome 扩展 → 整轮重派 AE/BE/CE 取截图 + DOM 二确；(2) 修 B 类真实缺陷（BE1-F1 未修 / BE3-F3 未修 / CE2-F1·F2 browser 复验）；(3) 三 critic 无 CRITICAL 漏测后方可收官。

---

## 9. 全套件绿门

收官前串行跑完（`test:server` 完全退出 → `test:web`，绝不并发）：

| 套件 | 结果 |
|---|---|
| `npm run test:server` | **GREEN** — 168 files / 1565 passed / 1 skipped，20.39s，0 残留 vitest |
| `npm run test:web` | **GREEN** — 231 files / 1610 passed，13.07s，0 残留 vitest |

跑完 `pgrep -f vitest | wc -l` = **0**（≤3 ✓）。相较 preflight baseline（server 1544 / web 1592）测试数上升，系回流轮新增确定性覆盖（BE3-F1 :memory: 账本用例、`dispatchViewerAction.test.ts`、A6/A7 CI 覆盖、C3 WsBridge 集成）——全绿，既有套件不破。
