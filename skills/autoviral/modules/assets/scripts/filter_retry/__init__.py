"""filter_retry — programmatic dispatcher for content-filter recovery.

See ``capabilities/filter-retries.md`` for the domain model (signatures
A through E plus ``unknown``). This package houses the runtime helpers
that translate that markdown decision tree into executable form.
"""
