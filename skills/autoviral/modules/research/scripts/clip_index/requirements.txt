# Phase 8.1 CLIP semantic search dependencies. Install with:
#   pip install -r skills/autoviral/modules/research/scripts/clip_index/requirements.txt
#
# Note: torch / faiss-cpu / Pillow / numpy are typically already installed via
# other modules. Only `open_clip_torch` is the new install for this module.
# When `open_clip` is missing, build_index.py and search.py degrade to stub
# mode (per D4) — they emit `{ "stub": true, ... }` JSON and exit 0 instead of
# crashing. This is by design so the UI can render an "install CLIP" hint.
open_clip_torch>=2.24.0
torch>=2.0
Pillow>=10.0
faiss-cpu>=1.7.4
numpy>=1.24
