export function AITab(_props: { workId: string }) {
  void _props;
  return (
    <div
      style={{
        fontFamily: "var(--font-mono)",
        fontSize: 11,
        color: "var(--text-dimmer)",
      }}
    >
      AI controls coming up.
    </div>
  );
}
